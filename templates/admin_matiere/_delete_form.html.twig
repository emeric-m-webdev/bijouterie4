<form method="post" class="d-inline-block" action="{{ path('app_admin_matiere_delete', {'id': matiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matiere.id) }}">
    <button class="btn p-0"><i class="text-danger" data-feather="trash-2"></i></button>
</form>
