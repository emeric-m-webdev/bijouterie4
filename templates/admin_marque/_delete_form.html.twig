<form method="post" class="d-inline-block" action="{{ path('app_admin_marque_delete', {'id': marque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.id) }}">
    <button class="btn p-0"><i class="text-danger" data-feather="trash-2"></i></button>
</form>
